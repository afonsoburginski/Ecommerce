/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['image.tmdb.org', 'owyaxdsnwzcavzstoxbi.supabase.co', 'placeholder.com'], // Add 'placeholder.com'
  },
};

export default nextConfig;
